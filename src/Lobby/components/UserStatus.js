import React from 'react'

function UserStatus() {

    // polling system that checks userdb ready status until both are true
    // updates user status below

    return (
        <div>
            <h1>user1 status</h1>
            <h1>user2 status</h1>
        </div>
    )
}

export default UserStatus
